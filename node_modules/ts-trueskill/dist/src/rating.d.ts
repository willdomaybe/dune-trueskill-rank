import { SkillGaussian } from './mathematics.js';
export declare class Rating extends SkillGaussian {
    constructor(mu?: number | SkillGaussian | [number, number], sigma?: number);
    toString(): string;
}
